import { describe, expect, it } from 'vitest';

import { mount } from "@vue/test-utils";
import ChooserGridVue from '../components/ChooserGrid.vue';
import AppVue from '../App.vue';

describe('ChooserGrid', () => {
    it("Renders Home", () => {
        // always succeed
        expect(true).toBe(true);
    });
});
